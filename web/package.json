{
  "name": "edge-detection-web-viewer",
  "version": "1.0.0",
  "description": "Web-based viewer for edge detection frames",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npx http-server . -p 8080",
    "dev": "npm run build && npm run serve"
  },
  "keywords": [
    "opencv",
    "edge-detection",
    "viewer"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "opencv-ts": "^1.2.9"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
